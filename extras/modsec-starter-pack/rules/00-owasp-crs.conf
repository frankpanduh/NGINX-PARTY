# OWASP Core Rule Set minimal example for starter pack
# Real deployments should update CRS regularly

# Prevent SQL Injection
SecRule ARGS "@rx select.+from" "id:900001,phase:2,log,msg:'Potential SQLi detected'"
# Prevent XSS
SecRule ARGS "@rx <script>" "id:900002,phase:2,log,msg:'Potential XSS detected'"

# Add more OWASP CRS rules as needed
